import StockSchema from '../models/stockitem';
import { Request, Response } from 'express';

export default class InventoryController {
    public async addOrder(req: Request, res: Response): Promise<void> {
    }
}